using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class Gridder : MonoBehaviour
{
    [SerializeField] GameObject ingredient;
    [SerializeField] float gridWidth;
    [SerializeField] float gridHeight;

    //array for the fruit prefabs
    [SerializeField] GameObject[] ingredients;

    // Start is called before the first frame update
    void Start()
    {
        
        // for loop for the grids width
        // for loop for the grids height
        //instantiate the ingredient prefab within a grid
        //set the grid to width and height given

    }
}
